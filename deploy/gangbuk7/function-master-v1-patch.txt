<style>
/* TAKY FUNCTION MASTER v1 */
.taky-modal{position:fixed;inset:7vh 8vw;background:#fff;border:1px solid #9fc5df;border-radius:12px;z-index:80;box-shadow:0 24px 70px #1235;display:none;overflow:auto}.taky-modal.open{display:block}.taky-modal-head{position:sticky;top:0;background:#f4f9fd;border-bottom:1px solid #c9deec;padding:12px 16px;display:flex;justify-content:space-between;align-items:center;z-index:2}.taky-modal-body{padding:14px 16px}.taky-list{width:100%;border-collapse:collapse;font-size:10px}.taky-list th,.taky-list td{border-bottom:1px solid #dfeaf2;padding:7px;text-align:left}.taky-list th{background:#f4f8fb}.critical .gbar{box-shadow:0 0 0 2px #143d63}.file .unlinked{opacity:.38;cursor:not-allowed}.toast{position:fixed;right:22px;bottom:22px;background:#103c5d;color:#fff;padding:10px 14px;border-radius:8px;z-index:100;font-size:10px;box-shadow:0 8px 24px #0003}
</style>
<script>
(function(){
const q=s=>document.querySelector(s), qa=s=>[...document.querySelectorAll(s)];
function toast(t){let n=document.createElement('div');n.className='toast';n.textContent=t;document.body.appendChild(n);setTimeout(()=>n.remove(),2200)}
function ensureModal(){if(q('#takyModal'))return;let m=document.createElement('section');m.id='takyModal';m.className='taky-modal';m.innerHTML='<div class="taky-modal-head"><b id="takyModalTitle">보기</b><button class="btn" id="takyModalClose">닫기</button></div><div id="takyModalBody" class="taky-modal-body"></div>';document.body.appendChild(m);q('#takyModalClose').onclick=()=>m.classList.remove('open')}
function openModal(title,html){ensureModal();q('#takyModalTitle').textContent=title;q('#takyModalBody').innerHTML=html;q('#takyModal').classList.add('open')}
function esc2(x){return String(x??'').replace(/[&<>"']/g,c=>({'&':'&amp;','<':'&lt;','>':'&gt;','"':'&quot;',"'":'&#39;'}[c]))}
function btnByText(t){return qa('button').find(b=>b.textContent.trim()===t)}
window.showStageList=function(){openModal('전체 단계 목록','<table class="taky-list"><tr><th>No</th><th>단계명</th><th>주관기관</th><th>기간</th><th>상태</th><th>시작</th><th>종료</th></tr>'+doc.stages.map(s=>'<tr data-stage="'+s.id+'"><td>'+String(s.id).padStart(2,'0')+'</td><td><button class="btn" onclick="document.querySelector(\'#takyModal\').classList.remove(\'open\');openEdit('+s.id+')">'+esc2(s.name)+'</button></td><td>'+esc2(s.authority)+'</td><td>'+s.duration+'개월</td><td>'+esc2(s.status)+'</td><td>'+esc2(s.start)+'</td><td>'+esc2(s.end)+'</td></tr>').join('')+'</table>')}
window.showSettings=function(){let compact=localStorage.getItem('gb7_compact')==='1';openModal('화면 설정','<label style="display:flex;gap:8px;align-items:center;font-size:11px"><input id="compactToggle" type="checkbox" style="width:auto" '+(compact?'checked':'')+'> 고밀도 표시</label><p style="font-size:9px;color:#678">설정은 이 브라우저에 저장됩니다.</p>');q('#compactToggle').onchange=e=>{localStorage.setItem('gb7_compact',e.target.checked?'1':'0');document.body.classList.toggle('gb7-compact',e.target.checked);toast('화면 설정 저장 완료')}}
function applyCompact(){document.body.classList.toggle('gb7-compact',localStorage.getItem('gb7_compact')==='1')}
window.toggleCritical=function(){let on=document.body.classList.toggle('critical');toast(on?'주요경로 강조 ON':'주요경로 강조 OFF')}
function addMonths(date,n){let d=new Date(date+'T00:00:00');d.setMonth(d.getMonth()+Math.max(0,Number(n)||0));return d.toISOString().slice(0,10)}
window.autoSchedule=function(){if(!doc.stages?.length)return;let s=doc.stages, first=s[0].start||new Date().toISOString().slice(0,10);s[0].start=first;s[0].end=addMonths(first,s[0].duration);for(let i=1;i<s.length;i++){let x=s[i];if([4,5,6].includes(x.id)){let base=s.find(z=>z.id===3);x.start=base.end;x.end=addMonths(x.start,x.duration)}else if(x.id===7){let ends=s.filter(z=>[4,5,6].includes(z.id)).map(z=>z.end).sort();x.start=ends[ends.length-1];x.end=addMonths(x.start,x.duration)}else{let p=s[i-1];x.start=p.end;x.end=addMonths(x.start,x.duration)}}renderFlow();renderGantt();renderStats();toast('자동 일정계산 완료 · 저장 버튼으로 확정')}
window.downloadSchedule=function(){let rows=[['No','단계명','주관기관','예상기간(개월)','상태','시작일','종료일'],...doc.stages.map(s=>[s.id,s.name,s.authority,s.duration,s.status,s.start,s.end])];let csv='\ufeff'+rows.map(r=>r.map(v=>'"'+String(v??'').replace(/"/g,'""')+'"').join(',')).join('\r\n');let a=document.createElement('a');a.href=URL.createObjectURL(new Blob([csv],{type:'text/csv;charset=utf-8'}));a.download='강북7구역_인허가_일정.csv';a.click();setTimeout(()=>URL.revokeObjectURL(a.href),1000);toast('일정 CSV 다운로드')}
function wire(){let b=btnByText('목록보기');if(b)b.onclick=showStageList;b=btnByText('설정');if(b)b.onclick=showSettings;b=btnByText('주요경로 보기');if(b)b.onclick=toggleCritical;b=btnByText('자동 일정계산');if(b)b.onclick=autoSchedule;b=btnByText('다운로드');if(b)b.onclick=downloadSchedule;let memo=q('.memo'),save=q('.savewide');if(memo){memo.value=localStorage.getItem('gb7_memo')||''}if(save)save.onclick=()=>{localStorage.setItem('gb7_memo',memo.value);toast('메모 저장 완료')};qa('.file').forEach(f=>{let spans=f.querySelectorAll('span');[spans[2],spans[3]].forEach(x=>{if(x){x.classList.add('unlinked');x.title='원본 파일 경로가 연결되지 않았습니다';x.onclick=()=>toast('관련 파일 원본 연결 필요')}})});applyCompact()}
if(document.readyState==='loading')document.addEventListener('DOMContentLoaded',wire);else wire();
})();
</script>