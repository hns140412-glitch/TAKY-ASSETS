0-24 10z"/><path d="M29 29l9 9M12 36l7 7"/>',11:'<path d="M24 5v38M7 11h34M12 11v7M36 11v7M17 43h14"/><path d="M24 11l12 7"/>',12:'<path d="M8 29h7l18-8v16l-18-8H8z"/><path d="M33 24l7-4M33 34l7 4M15 30l3 10"/>',13:'<path d="M9 42V9h30v33M5 42h38"/><path d="M15 15h5v5h-5zM28 15h5v5h-5zM15 26h5v5h-5zM28 26h5v5h-5zM21 42V33h6v9"/>'}[id]||'';return `<svg class="ico" ${common}>${p}</svg>`}
function card(s,cls='big'){return `<div class="card ${cls}" data-id="${s.id}" onclick="openEdit(${s.id})"><div class="num">${String(s.id).padStart(2,'0')}</div>${ico(s.id)}<h3>${esc(s.name)}</h3><p>${esc(s.summary)}</p><span class="agency">${esc(s.authority)}</span><div class="pill">약 ${s.duration}개월</div></div>`}
function renderFlow(){let b=id=>doc.stages.find(x=>x.id===id),z=$('lanes');z.innerHTML=`<div class="lane plan"><div class="ltitle">① 계획수립<small>(약 2개월)</small></div>${card(b(1))}<i class="arrow"></i></div><div class="lane permit"><div class="ltitle">② 인허가 심의<small>(약 6 ~ 10개월)</small></div><div class="permitgrid">${card(b(2))}${card(b(3))}</div><i class="arrow"></i></div><div class="lane parallel"><div class="parallel-label">통합심의 이후 병행(강북구)</div><div class="parallelgrid">${card(b(4),'')}${card(b(5),'')}${card(b(6),'')}</div><div class="merge">04 · 05 · 06 완료 후 → 07 진행 가능</div><i class="arrow"></i></div><div class="lane execute"><div class="ltitle">③ 사업시행<small>(약 2개월)</small></div>${card(b(7))}<i class="arrow"></i></div><div class="lane dispose"><div class="ltitle">④ 관리처분<small>(약 3~4개월)</small></div>${card(b(8),'')}${card(b(9),'')}<i class="arrow"></i></div><div class="lane move"><div class="ltitle">⑤ 이주/철거<small>(약 8~12개월)</small></div>${card(b(10))}<i class="arrow"></i></div><div class="lane demo"><div class="num">10</div>${ico(10)}<b>이주·철거</b><span class="agency">조합 / 시공사</span><i class="arrow"></i></div><div class="lane build"><div class="ltitle">⑥ 착공/분양<small>(약 4~6개월)</small></div>${card(b(11),'')}${card(b(12),'')}${card(b(13),'')}</div>`}
function renderDetail(s){$('detail').innerHTML=`<div class="dhead"><div class="dnum">${String(s.id).padStart(2,'0')}</div><div><h2>${esc(s.name)}</h2><p>${esc(s.authority)} · 약 ${s.duration}개월</p></div></div><div class="dtabs"><span>기본정보</span><span>상세일정</span><span>업무절차</span><span>관련문서</span><span>이슈/리스크</span></div><div class="dbody"><div class="info"><b>단계번호</b><span>${String(s.id).padStart(2,'0')}</span><b>구분</b><span>${esc(s.phase)}</span><b>예상기간</b><span>약 ${s.duration}개월</span><b>주관기관</b><span>${esc(s.authority)}</span><b>선행조건</b><span>${esc(s.condition)}</span><b>후행조건</b><span>${s.id===5?'구조안전심의 완료 후 사업시행계획인가':'다음 단계 조건 확인'}</span><b>상태</b><span><i class="status">${esc(s.status)}</i></span><b>설명</b><span>${esc(s.summary)}</span></div><div class="sectiontitle"><span>주요 업무 절차</span><button class="btn" onclick="openEdit(${s.id})">수정하기</button></div><table class="task"><tr><th>#</th><th>업무내용</th><th>예상기간</th><th>담당</th><th>상태</th></tr>${(s.tasks||[]).map((t,i)=>`<tr><td>${i+1}</td><td>${esc(t.name)}</td><td>${esc(t.duration)}</td><td>${esc(t.owner)}</td><td>${esc(t.status)}</td></tr>`).join('')}</table></div>`}
function mi(d){let x=new Date(d+'T00:00:00');return x.getFullYear()*12+x.getMonth()}function renderGantt(){let v=doc.stages.filter(s=>s.start&&s.end),g=$('gantt');let mn=Math.min(...v.map(s=>mi(s.start))),mx=Math.max(...v.map(s=>mi(s.end))),sp=Math.max(1,mx-mn+1);g.innerHTML='<div class="grow head"><div class="gc">No</div><div class="gc">단계명</div><div class="gc">주관기관</div><div class="gc">예상기간</div><div class="gc">일정</div></div>';v.forEach(s=>{let l=(mi(s.start)-mn)/sp*100,w=(mi(s.end)-mi(s.start)+1)/sp*100,c=s.id>=4&&s.id<=6?'green':s.id===7?'purple':s.id>=8&&s.id<=9?'orange':s.id===10?'red':s.id>=11?'dark':'';g.innerHTML+=`<div class="grow"><div class="gc">${String(s.id).padStart(2,'0')}</div><div class="gc">${esc(s.name)}</div><div class="gc">${esc(s.aut