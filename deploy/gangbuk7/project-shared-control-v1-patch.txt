<style>
/* TAKY PROJECT SHARED CONTROL v1 */
.pc-shared .pc-tabs{position:sticky;top:0;background:#fff;z-index:4}.pc-shared .pc-panel{display:none}.pc-shared .pc-panel.on{display:block}.pc-formbar{display:flex;gap:4px;flex-wrap:wrap;justify-content:flex-end}.pc-smallbtn{border:1px solid #b9d1e1;background:#fff;color:#176b9c;border-radius:5px;padding:4px 6px;font-size:7px;font-weight:900;cursor:pointer}.pc-smallbtn.primary{background:#0c73ad;color:#fff;border-color:#0c73ad}.pc-smallbtn.danger{color:#b13b31;border-color:#e4b9b4}.pc-listrow{display:grid;grid-template-columns:68px 1fr 48px;gap:5px;align-items:start;font-size:7.2px;padding:5px 0;border-bottom:1px solid #e3ebf0}.pc-listrow b{color:#176b9c}.pc-listrow em{font-style:normal;color:#738896}.pc-issue{display:grid;grid-template-columns:42px 1fr 54px;gap:5px;align-items:start;font-size:7.2px;padding:5px 0;border-bottom:1px solid #e3ebf0}.pc-doc{display:grid;grid-template-columns:1fr 55px 48px;gap:5px;align-items:center;font-size:7.2px;padding:5px 0;border-bottom:1px solid #e3ebf0}.pc-gantt-legend{display:flex;gap:10px;justify-content:flex-end;font-size:6.8px;margin:3px 2px 5px;color:#637d90}.pc-gantt-legend i{display:inline-block;width:18px;height:4px;margin-right:3px;vertical-align:middle;border-radius:3px}.pc-gantt-legend .p{background:#93b6cf}.pc-gantt-legend .a{background:#153f60}.pc-gantt-legend .f{background:#e29a31}.timeline .pcbar-actual,.timeline .pcbar-forecast{position:absolute;height:5px;border-radius:5px;z-index:3}.timeline .pcbar-actual{top:11px;background:#153f60}.timeline .pcbar-forecast{top:18px;background:#e29a31}.pc-save-state{font-size:6.7px;color:#6e8798;margin-top:5px}.pc-save-state.ok{color:#15735f}.pc-save-state.warn{color:#b05f14}
</style>
<script>
(function(){
const refDocs={5:['건축물 안전영향평가 보고서','확정심의 신청서','설계설명서','이전심의 조치계획서','도면(건축·구조·토목)','구조계산서','구조안전 및 내진설계 확인서','토목 흙막이계산서','역타공법 구조계산서','성능기반 내진설계보고서','지반조사보고서','풍동실험보고서','특기시방서']};
function E(x){return String(x??'').replace(/[&<>"']/g,c=>({'&':'&amp;','<':'&lt;','>':'&gt;','"':'&quot;',"'":'&#39;'}[c]))}
function ensure(){doc.projectControl=doc.projectControl||{};doc.projectControl.history=Array.isArray(doc.projectControl.history)?doc.projectControl.history:[];doc.projectControl.issues=Array.isArray(doc.projectControl.issues)?doc.projectControl.issues:[];doc.projectControl.documents=Array.isArray(doc.projectControl.documents)?doc.projectControl.documents:[];}
function byStage(a,id){return a.filter(x=>Number(x.stageId)===Number(id))}
function toast(t){let n=document.createElement('div');n.className='toast';n.textContent=t;document.body.appendChild(n);setTimeout(()=>n.remove(),2200)}
async function persist(){ensure();try{let r=await api('/api/data',{method:'POST',body:JSON.stringify(doc)});if(!r.ok)throw new Error('HTTP '+r.status);let saved=await r.json();if(!saved.projectControl)throw new Error('SERVER_SCHEMA');doc=saved;document.querySelector('#updated')&&(document.querySelector('#updated').textContent=`공용 저장 완료 | ${doc.updated_at||'-'} by Taky`);toast('프로젝트 공용 데이터 저장 완료');return true}catch(e){toast('공용 저장 실패 · 서버 데이터 구조 확인 필요');return false}}
function ask(label,def=''){let v=prompt(label,def);return v===null?null:v.trim()}
window.pcEditSchedule=async function(id){let s=doc.stages.find(x=>x.id===id);if(!s)return;let a1=ask('ACTUAL 시작일 (YYYY-MM-DD, 비우면 미등록)',s.actualStart||'');if(a1===null)return;let a2=ask('ACTUAL 완료일 (YYYY-MM-DD, 진행중이면 비워도 됨)',s.actualEnd||'');if(a2===null)return;let f1=ask('FORECAST 시작일 (YYYY-MM-DD)',s.forecastStart||'');if(f1===null)return;let f2=ask('FORECAST 완료일 (YYYY-MM-DD)',s.forecastEnd||'');if(f2===null)return;s.actualStart=a1;s.actualEnd=a2;s.forecastStart=f1;s.forecastEnd=f2;if(await persist()){renderGantt();renderDetail(s)}}
window.pcAddHistory=async function(id){ensure();let date=ask('추진일자 (YYYY-MM-DD)',new Date().toISOString().slice(0,10));if(!date)return;let type=ask('구분 (회의/심의/보완/결정/제출/이슈)','회의');if(type===null)return;let text=ask('추진내용');if(!text)return;let impact=ask('일정영향/조건 (없으면 비워도 됨)','');if(impact===null)return;let owner=ask('담당/주관','');if(owner===null)return;doc.projectControl.history.push({id:'H'+Date.now(),stageId:id,date,type,text,impact,owner,source:'project'});if(await persist())renderDetail(doc.stages.find(x=>x.id===id))}
window.pcAddIssue=async function(id){ensure();let title=ask('이슈/리스크 제목');if(!title)return;let severity=ask('심각도 (HIGH/MID/LOW)','MID');if(severity===null)return;let impact=ask('일정/후속단계 영향','');if(impact===null)return;let action=ask('대응조치','');if(action===null)return;let owner=ask('담당','');if(owner===null)return;let due=ask('대응기한 (YYYY-MM-DD)','');if(due===null)return;doc.projectControl.issues.push({id:'R'+Date.now(),stageId:id,title,severity:severity.toUpperCase(),impact,action,owner,due,status:'OPEN',source:'project'});if(await persist())renderDetail(doc.stages.find(x=>x.id===id))}
window.pcAddDocument=async function(id){ensure();let name=ask('제출서류/준비자료명');if(!name)return;let classification=ask('분류 (확정/확인필요/사례참고)','확인필요');if(classification===null)return;let status=ask('준비상태 (미착수/준비중/검토중/완료)','미착수');if(status===null)return;let owner=ask('작성/준비 담당','');if(owner===null)return;let due=ask('목표일 (YYYY-MM-DD)','');if(due===null)return;doc.projectControl.documents.push({id:'D'+Date.now(),stageId:id,name,classification,status,owner,due,source:classification==='사례참고'?'case':'project'});if(await persist())renderDetail(doc.stages.find(x=>x.id===id))}
window.pcCycleDocument=async function(id){ensure();let d=doc.projectControl.documents.find(x=>x.id===id);if(!d)return;let seq=['미착수','준비중','검토중','완료'];d.status=seq[(seq.indexOf(d.status)+1)%seq.length];if(await persist())renderDetail(doc.stages.find(x=>x.id===Number(d.stageId)))}
window.pcCloseIssue=async function(id){ensure();let r=doc.projectControl.issues.find(x=>x.id===id);if(!r)return;r.status=r.status==='CLOSED'?'OPEN':'CLOSED';if(await persist())renderDetail(doc.stages.find(x=>x.id===Number(r.stageId)))}
function sharedHTML(s){ensure();let hs=byStage(doc.projectControl.history,s.id).sort((a,b)=>(a.date||'').localeCompare(b.date||'')),is=byStage(doc.projectControl.issues,s.id),ds=byStage(doc.projectControl.documents,s.id),refs=refDocs[s.id]||[];let done=ds.filter(x=>x.status==='완료').length,ready=ds.length?Math.round(done/ds.length*100):0;return `<div class="pc-tabhost pc-shared"><div class="pc-tabs"><button class="pc-tab on" data-p="sum">관제요약</button><button class="pc-tab" data-p="sch">PLAN/ACTUAL</button><button class="pc-tab" data-p="doc">제출서류</button><button class="pc-tab" data-p="risk">이슈/리스크</button><button class="pc-tab" data-p="hist">추진이력</button></div><section class="pc-panel on" data-panel="sum"><div class="pc-kpis"><div class="pc-kpi"><b>${E(s.status||'예정')}</b><span>현재 상태</span></div><div class="pc-kpi"><b>${ready}%</b><span>등록서류 준비율</span></div><div class="pc-kpi"><b>${is.filter(x=>x.status!=='CLOSED').length}</b><span>OPEN 이슈</span></div></div><div class="pcbox"><h4>데이터 출처 원칙</h4><div class="pcline"><b>회사표준</b><span>회사 매뉴얼 확인 후 확정</span></div><div class="pcline"><b>강북7</b><span>실제 운영·일정·이력·이슈</span></div><div class="pcline"><b>사례</b><span>타 프로젝트 사례는 참고/예측 근거</span></div></div></section><section class="pc-panel" data-panel="sch"><div class="pc-actionbar"><strong>PLAN / ACTUAL / FORECAST</strong><div class="pc-formbar"><button class="pc-smallbtn primary" onclick="pcEditSchedule(${s.id})">실제·전망 입력</button></div></div><div class="pcbox"><div class="pcline"><b>PLAN</b><span>${E(s.start||'-')} → ${E(s.end||'-')}</span></div><div class="pcline"><b>ACTUAL</b><span>${E(s.actualStart||'-')} → ${E(s.actualEnd||'-')}</span></div><div class="pcline"><b>FORECAST</b><span>${E(s.forecastStart||'-')} → ${E(s.forecastEnd||'-')}</span></div><div class="pcline"><b>Dependency</b><span>${E(s.condition||'확인 필요')}</span></div><div class="source-rule">PLAN은 기준선으로 보존. ACTUAL과 FORECAST만 별도 입력하여 계획을 덮어쓰지 않는다.</div></div></section><section class="pc-panel" data-panel="doc"><div class="pc-actionbar"><strong>프로젝트 제출서류</strong><button class="pc-smallbtn primary" onclick="pcAddDocument(${s.id})">+ 등록</button></div>${ds.length?ds.map(d=>`<div class="pc-doc"><span>${E(d.name)}<br><em>${E(d.owner||'담당 미지정')} · ${E(d.due||'기한 미지정')}</em></span><span class="docstate">${E(d.status)}</span><button class="pc-smallbtn" onclick="pcCycleDocument('${d.id}')">상태변경</button></div>`).join(''):'<div class="pc-empty">확정 프로젝트 서류 미등록</div>'}${refs.length?`<div class="pcbox"><h4>사례참고 · 확인필요</h4>${refs.map(x=>`<div class="docrow"><span>${E(x)}</span><span class="docstate">확인필요</span><span>사례참고</span></div>`).join('')}<div class="pc-source">※ 강북7 확정 제출목록 아님. 회사 매뉴얼/관할기관 확인 전 표준으로 승격하지 않음.</div></div>`:''}</section><section class="pc-panel" data-panel="risk"><div class="pc-actionbar"><strong>프로젝트 이슈 / 리스크</strong><button class="pc-smallbtn primary" onclick="pcAddIssue(${s.id})">+ 등록</button></div>${is.length?is.map(r=>`<div class="pc-issue"><span class="riskbadge ${r.severity==='HIGH'?'high':''}">${E(r.severity)}</span><span><b>${E(r.title)}</b><br><em>${E(r.impact||'영향 미입력')} · 대응: ${E(r.action||'-')} · ${E(r.owner||'담당 미지정')}</em></span><button class="pc-smallbtn ${r.status==='CLOSED'?'':'danger'}" onclick="pcCloseIssue('${r.id}')">${r.status==='CLOSED'?'재오픈':'종결'}</button></div>`).join(''):'<div class="pc-empty">등록된 실제 이슈/리스크 없음</div>'}</section><section class="pc-panel" data-panel="hist"><div class="pc-actionbar"><strong>실제 추진이력</strong><button class="pc-smallbtn primary" onclick="pcAddHistory(${s.id})">+ 이력등록</button></div>${hs.length?hs.map(h=>`<div class="pc-listrow"><b>${E(h.date)}</b><span><strong>${E(h.type||'이력')}</strong> · ${E(h.text)}<br><em>${E(h.impact||'일정영향 없음/미입력')} · ${E(h.owner||'담당 미지정')}</em></span><span>실제</span></div>`).join(''):'<div class="pc-empty">실제 추진경위 미등록</div>'}<div class="source-rule">실제 이력은 계획일정과 분리해 누적하며 향후 유사 프로젝트 사례 데이터로 재사용 가능하게 유지.</div></section></div>`}
function install(s){let body=document.querySelector('#detail .dbody');if(!body)return;body.querySelectorAll('.pc-tabhost').forEach(x=>x.remove());body.insertAdjacentHTML('beforeend',sharedHTML(s));let host=body.querySelector('.pc-shared');host.querySelectorAll('.pc-tab').forEach(b=>b.onclick=()=>{host.querySelectorAll('.pc-tab').forEach(x=>x.classList.toggle('on',x===b));host.querySelectorAll('.pc-panel').forEach(p=>p.classList.toggle('on',p.dataset.panel===b.dataset.p))})}
const oldDetail=window.renderDetail;window.renderDetail=function(s){ensure();oldDetail(s);install(s)};
function month(d){let x=new Date(d+'T00:00:00');return x.getFullYear()*12+x.getMonth()}
const oldGantt=window.renderGantt;window.renderGantt=function(){oldGantt();let stages=doc.stages.filter(s=>s.start&&s.end),mn=Math.min(...stages.map(s=>month(s.start))),mx=Math.max(...stages.map(s=>month(s.end))),sp=Math.max(1,mx-mn+1);let rows=[...document.querySelectorAll('#gantt .grow')].slice(1);rows.forEach((row,i)=>{let s=stages[i],tl=row.querySelector('.timeline');if(!s||!tl)return;let plan=tl.querySelector('.gbar');if(plan)plan.classList.add('plan');function add(a,b,cl){if(!a||!b)return;let l=(month(a)-mn)/sp*100,w=(month(b)-month(a)+1)/sp*100;let n=document.createElement('div');n.className=cl;n.style.left=Math.max(0,l)+'%';n.style.width=Math.max(1,Math.min(100-l,w))+'%';tl.appendChild(n)}add(s.actualStart,s.actualEnd||s.actualStart,'pcbar-actual');add(s.forecastStart,s.forecastEnd||s.forecastStart,'pcbar-forecast')});let g=document.querySelector('#gantt');if(g&&!g.previousElementSibling?.classList.contains('pc-gantt-legend'))g.insertAdjacentHTML('beforebegin','<div class="pc-gantt-legend"><span><i class="p"></i>PLAN</span><span><i class="a"></i>ACTUAL</span><span><i class="f"></i>FORECAST</span></div>')}
const oldReload=window.reloadData;window.reloadData=async function(){await oldReload();ensure();renderGantt()};
})();
</script>