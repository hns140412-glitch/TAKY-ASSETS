hority)}</div><div class="gc">${s.duration}개월</div><div class="timeline"><div class="gbar ${c}" style="left:${l}%;width:${w}%"></div></div></div>`})}
function statIcon(n){let p=['<path d="M8 12h32v28H8zM14 6v12M34 6v12M8 20h32"/>','<path d="M10 8h28v32H10zM16 5v7M32 5v7M16 22h16M16 29h11"/>','<circle cx="24" cy="24" r="17"/><path d="M16 24l5 5 11-13"/>','<circle cx="24" cy="24" r="17"/><path d="M24 7v17h17"/>','<circle cx="17" cy="18" r="6"/><circle cx="31" cy="18" r="6"/><path d="M6 40c1-8 5-13 11-13M42 40c-1-8-5-13-11-13M18 40c1-8 4-13 6-13s5 5 6 13"/>','<path d="M9 42V10h30v32M5 42h38M15 17h5M28 17h5M15 25h5M28 25h5"/>','<path d="M24 6l18 34H6z"/><path d="M24 17v12M24 35v1"/>'][n];return `<svg viewBox="0 0 48 48" fill="none" stroke="currentColor" stroke-width="2.5" stroke-linecap="round" stroke-linejoin="round">${p}</svg>`}
function renderStats(){let done=doc.stages.filter(s=>s.status==='완료').length,prog=doc.stages.filter(s=>s.status==='진행중').length,wait=doc.stages.filter(s=>s.status==='예정').length;let a=[['약 25 ~ 37개월','총 예상기간'],['13개','전체 단계 수'],[done+'개','완료 단계'],[prog+'개','진행 단계'],[wait+'개','대기 단계'],['4개','관련 기관'],['3건','주요 이슈']];$('stats').innerHTML=a.map((x,i)=>`<div class="stat">${statIcon(i)}<div><b>${x[0]}</b><span>${x[1]}</span></div></div>`).join('')}
async function reloadData(){try{let r=await api('/api/data');doc=await r.json();renderFlow();renderGantt();renderStats();renderDetail(doc.stages.find(s=>s.id===5));$('updated').textContent=`최종 업데이트 | ${doc.updated_at||'-'} by Taky`}catch(e){$('updated').textContent='서버 연결 실패'}}
function openEdit(id){active=doc.stages.findIndex(x=>x.id===id);let s=doc.stages[active];$('edt').textContent=`${String(s.id).padStart(2,'0')} ${s.name}`;fn.value=s.name;fd.value=s.duration;fa.value=s.authority;fs.value=s.start;fe.value=s.end;fst.value=s.status;fsummary.value=s.summary;fc.value=s.condition;fno.value=s.note;renderTaskEdit(s.tasks||[]);drawer.classList.add('open');ov.classList.add('show');renderDetail(s)}function closeEdit(){drawer.classList.remove('open');ov.classList.remove('show')}function renderTaskEdit(a){taskedit.innerHTML='';a.forEach((t,i)=>taskedit.innerHTML+=`<div class="taskrow"><input data-n="${i}" value="${esc(t.name)}"><input data-d="${i}" value="${esc(t.duration)}"><input data-o="${i}" value="${esc(t.owner)}"><select data-s="${i}"><option ${t.status==='예정'?'selected':''}>예정</option><option ${t.status==='진행중'?'selected':''}>진행중</option><option ${t.status==='완료'?'selected':''}>완료</option></select><button class="btn" onclick="rmTask(${i})">x</button></div>`)}function readTasks(){return [...document.querySelectorAll('[data-n]')].map((x,i)=>({name:x.value.trim(),duration:document.querySelector(`[data-d="${i}"]`).value.trim(),owner:document.querySelector(`[data-o="${i}"]`).value.trim(),status:document.querySelector(`[data-s="${i}"]`).value})).filter(x=>x.name)}function rmTask(i){let a=readTasks();a.splice(i,1);renderTaskEdit(a)}function applyStage(){if(active===null)return;let s=doc.stages[active];Object.assign(s,{name:fn.value.trim(),duration:Number(fd.value||0),authority:fa.value.trim(),start:fs.value,end:fe.value,status:fst.value,summary:fsummary.value.trim(),condition:fc.value.trim(),note:fno.value.trim(),tasks:readTasks()});renderFlow();renderGantt();renderStats();renderDetail(s);closeEdit()}async function saveAll(){if(drawer.classList.contains('open'))applyStage();let r=await api('/api/data',{method:'POST',body:JSON.stringify(doc)});if(r.ok){doc=await r.json();$('updated').textContent=`저장 완료 | ${doc.updated_at} by Taky`}}function doSearch(){let q=$('search').value.trim().toLowerCase();document.querySelectorAll('.card').forEach(n=>{let s=doc.stages.find(x=>x.id===Number(n.dataset.id));if(!s)return;let t=[s.name,s.authority,s.summary].join(' ').toLowerCase();n.style.opacity=!q||t.includes(q)?1:.18})}
reloadData();
</script></body></html>